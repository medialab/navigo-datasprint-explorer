import React, { useState } from 'react';

export default function ControlBar ({
    
}) {
    return (
        <header style={{ marginBottom: '20px' }}>
            <h1 className='title'>Exploration des données Navigo pour le datasprint 2022</h1>
            <h2 className='subtitle'>Sélectionner les paramètres de la visualisation dans la colonne de gauche puis téléchargez la en cliquant sur le trois points en haut à droite.</h2>
        </header>
    );
}